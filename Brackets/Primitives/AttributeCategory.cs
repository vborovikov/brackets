namespace Brackets.Primitives
{
    public enum AttributeCategory
    {
        Name, // name-value pair, name part
        Value, // name-value pair, value part
        Flag, // just name, value is not expected
        // tag=value
    }
}
